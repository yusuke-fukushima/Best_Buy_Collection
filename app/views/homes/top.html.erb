<%= render 'shared/flash_messages' %>
<div class="mx-auto text-center">
<h1 class="mt-5">BEST BUY COLLECTION</h1>
<p>BEST BUY COLLECTIONはあなたの買って良かったものを投稿するアプリです。</p>
<div class="mx-auto text-center">
<%= link_to 'ゲストログイン（閲覧用）', users_guest_sign_in_path,class: 'mt-5 btn btn-success', method: :post %>
</div>
</div>
<div class="container">
  <div class="row no-gutters">
    <div class="col-12 col-sm-12 col-md-8 col-lg-5 col-xl-5 mx-auto text-center shadow-lg rounded px-0 my-5">
      <h4 class="text-dark bg-light p-4 mb-0"><strong>投稿一覧</strong></h4>
      <div class="form-body p-4 bg-light">
        <%= link_to "投稿する", new_item_path, class:"btn btn-block btn-success" %>
        <hr>
        <% @items.each do |item| %>
          <div>
            <div class="container">
              <div class="row no-gutters">
                <div class="col-4">
                  <%= link_to user_path(item.user) do %>
                    <div>
                      <%= attachment_image_tag(item.user, :profile_image, size: "100x100", fallback: "no_image.png") %>
                    </div>
                    <div>
                      <%= item.user.name %>
                    </div>
                  <% end %>
                </div>
                <div class="col-8">
                  <%= link_to item_path(item) do %>
                    <div>
                      <%= attachment_image_tag(item.user, :profile_image, size: "100x100", fallback: "no_image.png") %>
                    </div>
                    <div>
                      <%= item.purchase_time.strftime("%-m月") %>
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
          <hr>
        <% end %>
      </div>
    </div>
  </div>
</div>